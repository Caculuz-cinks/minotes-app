import 'package:flutter/material.dart';

class Note {
  Color color;
  String type, text;

  Note({ this.color, this.type, this.text});

  addNew(note)async{
//    print(note.text);
//    await notes.add(note);
//    print(notes.length);
    print("saved");
  }
  viewNote(){

  }
  allNotes(){
//    return notes;
  }
}